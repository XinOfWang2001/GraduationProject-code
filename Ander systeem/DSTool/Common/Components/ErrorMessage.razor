@if (Message.Length > 0)
{
	<MudAlert Severity="Severity.Warning" Variant="Variant.Filled" Dense="true" Class="my-2">@Message</MudAlert>
}

@code {
	[Parameter]
	public string Message { get; set; } = string.Empty;
}
